sudo: false
language: java

jdk:
  - oraclejdk8

addons:
  apt:
    packages:
      - oracle-java8-installer

install:
  - mvn install -DskipTests
script:
  - mvn test

notifications:
  email: false

branches:
  only:
    - master

